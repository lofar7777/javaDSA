//Divisors of a number
//To find the divisors of a number we have to traverse a loop from 1 to the squareRoot of the number.
int n = 36;
for(int i = 1; i<sqrt(n); i++){
  if(n%i == 0){
      System.out.println(i);
  }
  if(n/i!=i){
      System.out.println(n/i);
    }
}
we can store the factors in a arraylist and then print the after sorting the arraylist in java.
